version: '3'

services:
  app:
    build: .
    ports:
      - "5000:5000"

  monitor:
    build: ./monitor
    depends_on:
      - controller

  controller:
    build: ./controller
    ports:
      - "5001:5001"
